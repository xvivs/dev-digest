# client/docs/

Studio architecture that needs more than a `CLAUDE.md` line and is true only of
`@devdigest/web`. Long-lived: how the UI is built, not how to run it.

Point at a file here from `client/CLAUDE.md` with a `Read X when Y` line.

- `component-anatomy.md` — how a route, a feature component and a data hook fit
  together.

Not here: the route map (`../README.md`), plans for unbuilt work (`../specs/`),
debugging findings (`../INSIGHTS.md`), or cross-package decisions (`../../docs/`).
