{
  "name": "PR detail Files changed tab renders the seeded diff",
  "description": "From PR #482, open the Files changed tab and confirm the unified-diff viewer renders a seeded file path (src/config.ts). Exercises the refactored diff-viewer (DiffViewer → FileCard → CodeLine). Read-only seeded data (no model call). The seed holds two repos and listByWorkspace has no ORDER BY, so the flow guards that the home redirect landed on acme/payments-api, and opens the PR by title rather than by row position. See e2e/README.md.",
  "steps": [
    {
      "cmd": [
        "open",
        "{BASE}/"
      ],
      "label": "load the app root"
    },
    {
      "cmd": [
        "wait",
        "--url",
        "/pulls"
      ],
      "label": "land on the PR list"
    },
    {
      "cmd": [
        "wait",
        "--text",
        "acme/payments-api"
      ],
      "label": "guard: the home redirect landed on the demo repo — the seed holds TWO repos (acme/payments-api and xvivs/dev-digest) and ReposRepository.listByWorkspace has no ORDER BY, so which one `/` redirects to is not guaranteed by contract"
    },
    {
      "cmd": [
        "find",
        "text",
        "Add rate limiting to public API endpoints",
        "click"
      ],
      "label": "open the PR row"
    },
    {
      "cmd": [
        "wait",
        "--url",
        "/pulls/482"
      ],
      "label": "navigate to the PR detail route"
    },
    {
      "cmd": [
        "wait",
        "--load",
        "networkidle"
      ],
      "label": "PR detail data settles"
    },
    {
      "cmd": [
        "find",
        "role",
        "button",
        "click",
        "--name",
        "Files changed"
      ],
      "label": "switch to the Files changed tab"
    },
    {
      "cmd": [
        "wait",
        "--url",
        "tab=diff"
      ],
      "label": "diff tab is active in the URL"
    },
    {
      "cmd": [
        "wait",
        "--text",
        "src/config.ts"
      ],
      "label": "a seeded changed file renders in the diff viewer"
    }
  ]
}
