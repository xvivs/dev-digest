# reviewer-core/docs/

Engine behaviour that needs more than a `CLAUDE.md` line. Long-lived: what the
model is shown and what we accept back.

Point at a file here from `reviewer-core/CLAUDE.md` with a `Read X when Y` line.

- `prompt-contract.md` — prompt section order, untrusted-content handling, the
  grounding gate and score recomputation.

Not here: the pipeline overview and public API (`../README.md`), plans for
unbuilt work (`../specs/`), or debugging findings (`../INSIGHTS.md`).
